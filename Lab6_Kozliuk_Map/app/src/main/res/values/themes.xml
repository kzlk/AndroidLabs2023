<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Define your custom theme that inherits from a built-in theme -->
    <style name="CustomSpinnerTheme" parent="Widget.AppCompat.Spinner">
        <!-- Customize any attributes here, e.g., text color -->
        <item name="android:textColor">#000000</item>
    </style>
    <style name="ModernSpinnerStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@drawable/modern_spinner_background</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:theme">@style/ModernSpinnerTheme</item>
    </style>

    <style name="ModernSpinnerTheme">
        <item name="android:textColor">#333333</item>
        <item name="android:background">#FFFFFF</item>
    </style>
    <style name="spinnerItemStyle" parent="android:Widget.TextView.SpinnerItem">
    </style>

    <style name="spinnerDropDownItemStyle" parent="android:TextAppearance.Widget.TextView.SpinnerItem">
    </style>

    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication" />
</resources>